# 构建产物与装机时才生成的东西，都不进 git。

# 三个页面构建出来的地方
/dist/

# `server/pregenerate-plugins.mjs` 生成的插件登记表。
# **它是装机时该做的那一步的样子**，不是源码——真部署时由装机脚本按那个环境的
# 挂载前缀现生成一份。提交进来的话，换个前缀就会静静地对不上。
/assembled/

# 实测留下的截图与产物
/out/
